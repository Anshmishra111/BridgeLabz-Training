package com.fundoo.fundoonotes.entity;

/**
 * TypeOfNote — UC4
 * Enum representing the type of note.
 * TEXT, CHECKLIST, IMAGE, LINK.
 */
public enum TypeOfNote {
    TEXT,
    CHECKLIST,
    IMAGE,
    LINK
}
