# JSP view resolver prefix and suffix
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# Configure port
server.port=8080

# Context path to match the old setup
server.servlet.context-path=/MyGreetingsApp
